# ○输入法 Oime · 阶段记录（2026-09-11 晚 ~ 2026-09-12）

> 阶段版本：**vc34 → vc41**（0.9.24-oime → 0.9.31-oime）
> 本阶段两条主线：**按手机侧耗电报告做优化** + **复制条/横屏/工具栏三项交互定稿**
> 逐轮细节见 [DEVLOG.md](DEVLOG.md)，当前状态见 [BUILD_STATUS.md](BUILD_STATUS.md)

---

## 一、耗电优化（本阶段重点）

### 1.1 输入：手机侧两份报告

| 报告 | 关键数据 |
|---|---|
| `oime_battery_report.md`（v2，root 只读采集） | 18h31m 周期：Oime **80.3 mAh**，占实际放电 **2.0%**、占可建模功耗 5.6%，应用级 **第 5**；振动 **7168 次/166s**；熄屏 doze 仍 4.55 mAh CPU；持有 AudioIn；Fg Service 1h04m；后台 17h09m |
| `oime-battery-eta.md` | 同源数据，结论一致（79.8 mAh / 2.0%） |

### 1.2 PC 侧核实：**两条"P0"是误判，先排除再动手**

| 报告结论 | 核实 | 证据 |
|---|---|---|
| Fg Service 1h04m → 应 `stopSelf()` | ❌ **不成立** | 全项目无 `startForeground()`、无前台服务；这是系统给 IME 服务记的 procstate（`createdFromFg`/`BIND_INPUT_METHOD`）。IME self-stop 会被立刻重绑，反而更耗 |
| AudioIn 唤醒锁被持续持有 | ❌ **不成立** | 全项目**零 WakeLock**；`AudioRecord` 每会话结束都 `release()`。batterystats 的 UID 块是**累计获取记录**而非当前持有；`AudioIn` 实为 AudioFlinger 内部锁 |
| 17h 后台常驻是问题 | ⚠️ 部分误判 | 默认输入法被系统绑定常驻是设计使然；我们**无任何定时器**，实测空闲 30s = **0 ticks** |

**⇒ 结论**：报告的总量与排名可信，但可整改项收敛为三件（都是应用自身的代码）。

### 1.3 本阶段实际改动（vc40）

| 优先级 | 改动 | 细节 |
|---|---|---|
| **P0-1** | **振动统一入口 + 70ms 节流** | `HapticsManager.haptic(Type)`；`KeyboardScreen` **11 处** `performHapticFeedback`（6 LongPress + 5 TextHandleMove）全部改走它。**顺带修 bug：以前关掉「打字振动」，○ 环拖动/滑块/四向滑动照样振** |
| **P0-2** | **引擎懒初始化 + 解锁预热** | `onCreate` 不再加载 librime/建会话（熄屏期系统重绑 IME 时做的就是这件重活）；改到**首次弹出键盘**时初始化；注册 `ACTION_USER_PRESENT` 解锁预热，补回首弹延迟 |
| **P1** | **复制条开关**（设置 → 键盘） | 关闭后**不注册剪贴板监听、不读剪贴板**——隐私更干净，少一个后台回调唤醒源 |

**未做（附理由）**

- **AudioRecord 实例复用**：收益有限，且有麦克风状态残留风险
- **targetSdk 34 → 36**：新版本受限的四个机制（FGS / WakeLock / 闹钟 / Job）我们**一个都没用**，
  升级对耗电收益 ≈ 0，却要承担行为变更回归（edge-to-edge、insets、权限）。
  ✅ 唯一硬卡点「**16 KB 内存页**」已实测通过：`librime_jni.so` 与 sherpa-onnx 全部 5 个 `.so` 的
  PT_LOAD 对齐均为 `0x4000`。**结论：延后到功能冻结后再升**

### 1.4 验收 KPI（下一个放电周期复核）

| KPI | 改前 | 目标 |
|---|---|---|
| 振动次数 | 7168 次 / 18.5h | **≤1500** |
| 熄屏 doze CPU | 4.55 mAh | **≤2 mAh** |
| 后台 CPU 归因 | 22.9 mAh | **≤15 mAh** |
| 空闲静默（键盘隐藏） | 0 ticks / 30s ✅ | 保持 |

采集命令见 `oime-优化结论-PC侧核实.md` 第四节。

---

## 二、复制条：规则经过三次迭代才定稿

| 轮次 | 规则 | 为什么被推翻 |
|---|---|---|
| vc38 | **任意按键**消亡 + 组词即消亡 | 用户实测「打字怎么都不消失」——真因是 `readClipboard()` 只跳过"已上屏文本"，消亡后不留痕，任何剪贴板回调都会把内容塞回来 |
| vc39 | 修 `dismissedClip`（已消亡文本不再复活）+ 复制条**居中**、字号跟随工具栏 | 规则仍不对 |
| vc40 | 只有「点击上屏」+「**无候选时按退格**」两条途径 | 用户场景是**强制复制的无效内容**，被逼着先上屏才能清掉不合理 |
| **vc41（定稿）** | **点击 = 上屏；左右划动 = 消亡**；打字/组词不消亡 | ✅ |

实现要点：复制条 Box 上挂 `detectHorizontalDragGestures`（横向累计 > 40dp 触发），
拖动时 `change.consume()` 保证**不会同时触发点击上屏**；消亡写入 `dismissedClip` 防复活。

---

## 三、横屏键盘：6 套设计 → 选定 → 废弃

1. 依次出过 **L1 双侧分体 / L2 侧栏候选 / L3 全宽紧凑**（第一轮）→ 用户倾向分体
2. 再出 **L4 镜像错位分体 / L5 弧形分体+拇指球 / L6 中央功能岛**（第二轮）→ 选定 **L4 + B 键归右半**（vc36 实现）
3. **vc40 定稿：废弃分体**（`qwertyLand` 不再引用），横屏沿用竖屏布局，**只把键高限低 25%**
   （`keyHeightDp × 0.75`，钳位 26~48dp）——横屏不再"重新学一套键位"

> 教训：分体布局在横屏确实省拇指行程，但**重建肌肉记忆的成本高于收益**；
> 唯一必要的是"别让键盘吃掉大半个横屏"。

---

## 四、界面与交互（本阶段其余改动）

| 项 | 结果 |
|---|---|
| **工具栏白名单** | 从 17 项精简到 **11 项**：剪贴板 / 方案 / 数字 / emoji / 符号 / 设置 / 语音 / 收起 / 中英 / 撤回 / **重做**；默认**不显示任何工具**（只有 ○ 圆环） |
| **新增 redo** | `KeyAction.Redo` + redo 栈（撤回时把反向操作压入；任何新操作清空 redo 栈） |
| **中英图标** | 原来 fallback 到「方案」图标（重合）→ 新增专属 `OimeIcons.lang`（"文 A" 造型） |
| **○ 菜单** | 瘦回 9 项（方案组/方案管理/输入方案移出，统一到设置）；**图标全部换成 OimeIcons**（不再混 Material 图标） |
| **方案切换** | 从左上角 popup / 整屏面板 → **键盘下方中间的横向悬浮栏**，点击即切；背景 `funcKeyBg@80%` 灰半透明（与剪贴板同 chrome） |
| **菜单悬浮栏** | 父级/子级统一**左侧「← 返回」**，删掉右侧「↑ 关闭」 |
| **设置界面** | 大方块=强调色、右两方块=**功能键灰**、其余大项卡=**浅灰**（二级页卡片同浅灰，靠覆盖 `surfaceContainerLow` 一处生效）；状态卡改为**三行**（○输入法 / 运行状态 / 方案） |
| **键盘细节** | 第 3 行 ⇧/⌫ 回 1.5 宽且**边缘与上下行对齐**；九宫格首末列收窄、中间三列加宽；「00」键整串上屏；四向提示=字面方向（长按仍右上角）；全符号网格**暗色反色 + 四行排布**；语音动画改**长条波纹**（工具栏一半宽） |
| **悬浮窗** | 跟随光标（`requestCursorUpdates`）+ 同步候选 + 字号同步 + **前三码规则** |
| **○ 圆环** | 呼吸改 3.6s 正弦波（19.9 的三角波观感太快）；眼睛 5 种动作、间隔 2~9s；上滑应用栏可**下滑关闭** |
| **K 键括号** | `{Left}` 改用 `setSelection`（原 DPAD 事件多数 App 不理会，光标进不了括号中间） |

---

## 五、工程侧记录（本阶段沉淀）

1. **推送链路**：本地 `.git` 损坏 → 走 Git Data API 的「工作树」变体
   （`push_via_api_tree.py`：不读 index、自己 os.walk + 忽略规则）
2. **⚠️ 网页侧编辑会被覆盖**：用户曾在 GitHub 网页改 README（提交 `Update README.md`），
   被 tree 推送覆盖 → 现在**推送前先拉一遍远程最近提交**确认有无网页侧编辑
3. **提交信息用单行**：多行 + 中文引号会被 shell 拆断（`$'...': command not found`）
4. **扩展函数不能全限定名调用**（踩了两次）：`enableEdgeToEdge`（19.10）、
   `detectHorizontalDragGestures`（19.18）→ 必须 `import` 后直调
5. **文档同步**：每轮更新 `DEVLOG.md`（逐轮）+ `BUILD_STATUS.md`（现况）+ `README.md`（功能/借鉴来源）

---

## 六、待办 / 下一步

- [ ] **下一个放电周期复核 KPI**（振动次数、熄屏 doze CPU、后台 CPU）
- [ ] 用户实测反馈：横屏限高 25% 的手感、工具栏 11 项是否够用、复制条划动手势是否顺手
- [ ] 复制条划动消亡的 `OimeClip` 埋点日志（tag `OimeClip`）可在复现时确认路径
- [ ] 延后项：targetSdk 36 升级（含 16KB 页回归）、AudioRecord 实例复用


---

# 第二阶段：2026-09-12 全天（vc40 → vc46）

> 一天内推了 7 个版本。前半程做耗电优化落地，后半程是密集的交互修复——
> 其中**诊断方法论的突破**（本机 logcat 吞第三方日志 → 改用文件埋点）是今天最有价值的一条。

## 一、耗电优化落地（vc40，见上文第一节）

按手机侧两份报告的**核实结论**执行三项：振动统一入口 + 70ms 节流、引擎懒初始化 + 解锁预热、复制条开关。
报告里的 Fg Service / AudioIn 两条经源码核实属**系统带账**，未做无效改动。

## 二、横屏方案最终定稿（vc40）

6 套设计 → 选定 L4 分体 → **废弃**，只保留「键高限低 25%」。教训：**重建肌肉记忆的成本高于收益**。

## 三、本日新增功能（vc40 ~ vc45）

| 功能 | 说明 |
|---|---|
| **单手模式** | 键盘缩到 78% 贴左/右；工具栏「单手」= 开关；空白侧圆形箭头 ▶/◀ 切左右手 |
| **悬浮模式** | 键盘缩到 86%，顶部拖动条整体移动；位置持久化（详见下方"窗口级实现"） |
| **嵌入式编辑** | 编码/候选用 `setComposingText` 内嵌到文本框（首选 / 编码 / 输入码 / 无，默认关） |
| **工具栏白名单** | 精简到 11 项（+ **redo** 重做栈、中英专属图标、单手/悬浮两个新工具） |
| **括号自动居中** | 成对括号上屏后光标自动移到中间，**不再依赖用户写 `{Left}`** |
| **恢复备份** | 设置 → 关于 → 选 `Oime_backup_*.json` 按类型写回全部偏好 |
| 复制条 | 消亡规则最终定稿：**点击=上屏 / 左右划动=消亡**（打字不消亡） |
| 其他 | 菜单悬浮栏统一左侧返回、方案切换栏改键盘下方悬浮栏（灰半透明）、设置三档配色、状态卡三行、H 键英文 `_` |

## 四、🔍 本日最重要的方法论发现：**本机 logcat 只放行系统日志**

**症状**：连续三轮埋点全是"零日志"——进程在跑、版本正确、代码里日志点齐全，
但 `logcat -d | grep OimeXxx` 恒为 0 条；同时段系统组件日志有 200+ 条。

**结论**：ColorOS（Android 16）**吞掉第三方 App 的 `Log.d`**。也就是说，
"零埋点"不代表代码没跑，而是**诊断通道本身失效**——我一度在拿错工具查问题。

**对策**：新增 `com.azime.input.core.diag.Diag`——日志同时追加写
**`/sdcard/Download/oime_diag.log`**（>128KB 自清空），PC 端 `adb shell cat` 直接读。

**收益（立竿见影）**：换成文件埋点后**第一次就抓到铁证**：
```
[Sym] show   code=h ascii=true  list=[_]    ← 气泡列表过滤正确
[Sym] commit sym=—— ascii=false             ← 提交却变回中文那项
[Cursor] move delta=-1 setSelection=ok      ← 括号居中已生效
```

## 五、本日关键根因清单（都靠日志或源码定位，不是猜的）

| # | 现象 | 真因 | 修法 |
|---|---|---|---|
| 1 | H 键英文长按不出 `_`（气泡里看得见 `_`，点了出 `——`） | **Compose 闭包过期**：手势块 `pointerInput(code, longClick, page)` 的 key 不含 `asciiMode` → 切中英后闭包不重建，读到的仍是旧 `state`/旧列表 | `rememberUpdatedState` + 手势块内 6 处改读最新值 + `asciiMode` 进 pointerInput key |
| 2 | K 键自定义"不能完全覆盖原生配置" | 手势优先级 `longPressSymbols.isNotEmpty()` **先**于 `hasCustomLong`，而 K 的内置括号表恒非空 → 自定义永远进不来 | `popup`/`longClick` 任一非空即**完全替换**内置表 |
| 3 | 括号上屏后光标不居中 | 19.12 的 `setSelection` 在 **`beginBatchEdit()` 内**调用，batch 内 `getExtractedText` 是**过期快照** | 光标移动移到 `endBatchEdit()` 之后 + 回读校验 + DPAD 回落 |
| 4 | 悬浮键盘"往上拖就被 App 挡住""只看得见两行" | ① IME 窗口只有键盘高（画到窗外）；② 切换悬浮时未重设窗口；③ **漏了 `contentTopInsets`**（App 仍被顶起） | 内容撑到屏高（窗口按内容测量）＋ 切换即重设窗口 ＋ `contentTopInsets` 报满屏 ＋ `touchableRegion` 只圈键盘 |
| 5 | 悬浮只能在下半屏拖动 | IME 窗口是 **WRAP_CONTENT 按内容测量**的，`setLayout(MATCH_PARENT)` 被系统覆盖 | 让内容自身 `height(screenHeightDp - 48dp)`，窗口随之变高 |
| 6 | 复制条"打字也不消亡"（三轮反复） | 消亡只清 `clipText` 不留痕，任何剪贴板回调都会把它塞回来 → 最终按用户语义改为**划动消亡** | `dismissedClip` 记忆 + 划动手势 |
| 7 | 诊断日志全为空 | OEM 屏蔽第三方 `Log.d` | Diag 文件埋点（见上） |

## 六、工程侧沉淀（本日新增）

1. **诊断通道**：本机不要依赖 logcat → 用 `Diag` 写 `/sdcard/Download/oime_diag.log`，
   adb `cat` 直接读（这是本项目**必须记住**的一条）
2. **扩展函数不能全限定名调用**（今天又踩 1 次，累计 3 次）：`enableEdgeToEdge`、
   `detectHorizontalDragGestures`、`positionInWindow` → 必须 `import` 后直调
3. **Compose 手势闭包的 key 要包含它读的所有状态**，否则"看得见新值、点下去是旧值"
4. **IME 窗口尺寸由内容测量决定**，想让窗口多大就让内容多大
5. **验证闭环**：装机 → 用户操作 → PC 读埋点 → 定性 → 再改；
   今天靠它把 4 个"猜不动"的问题全部收敛

## 七、待办

- [ ] 用户实测确认：H 键英文 `_`、悬浮自由拖动、K 括号居中、嵌入候选居中
- [ ] 耗电 KPI 复核（振动次数 ≤1500、熄屏 doze CPU ≤2 mAh、后台 CPU ≤15 mAh）
- [ ] 延后项：targetSdk 36（16KB 页已实测通过）、AudioRecord 实例复用


---

# 第三阶段：2026-09-13（vc47 → vc52）

> 一天推 6 个版本。主题从"加功能"转向**打磨交互 + 修一致性问题**，
> 其中**复制条一共迭代了 4 次才定稿**——这条线本身就是一份"需求与实现逐步对齐"的样本。

## 一、复制条：4 次迭代的收敛过程

| 轮次 | 交互 | 为什么被推翻 |
|---|---|---|
| 19.24 | 长文本改用 `clipFull` 上屏（修 80 字截断） | ✅ 保留，但**副作用**：`lastCommittedClip` 仍是全文 → 判重失配 |
| 19.25 | 消亡判重统一 `take(80)` + 划动 24dp | 划动被认为"不够灵敏" |
| 19.27 | 划动阈值做成**设置项**（默认 12dp） | 用户实测仍不好用 |
| 19.28 | 内容**靠左** + 右侧 **✕ 消亡按钮** + **长按消亡** + **震动提示**，废弃划动 | 长按实测无效 |
| **19.29（定稿）** | **只保留 ✕ 按钮**（点击消亡 + 30ms 震动）；点内容 = 上屏 | ✅ |

**顺带修掉的两个真 bug**：
1. **长文本只能上屏 80 字**：显示与上屏共用了 `clipText = text.take(80)` → 新增 `clipFull` 存全文
2. **复制一条后再次复制不显示**：消亡/上屏的抑制规则（跳过 `lastCommittedClip`/`dismissedClip`）
   **对"真·复制事件"也生效** → 改 `readClipboard(fromUserCopy)`：监听回调永远显示，仅"弹键盘复读"才抑制

## 二、打字手感（性能，19.24）

用户反馈"粘滞、不清脆、跟不上手速"，做了三件事：
1. **引擎调用改单线程串行**（`Dispatchers.Default.limitedParallelism(1)`）——
   原来每次按键各起协程并发打 librime，而 librime 内部有全局锁 ⇒ 互等 + 顺序错乱（trime2 用独立 dispatch 线程同理）
2. **无变化不发射状态**（`updateFromResult` 内容一致时提前 return），省掉每次按键的整棵键盘重组
3. **触感节流 70ms → 40ms**（70ms 会在快打时丢振动）

## 三、一致性修复（视觉/布局）

| 问题 | 真因 | 修法 |
|---|---|---|
| 设置**二级页卡片是淡紫底**（主页是浅灰） | **M3 默认配色就是淡紫系**，卡片按不同中性色角色取色，19.15 只覆盖了 2 个角色 | 主题**全量覆盖中性色** + `SettingsActivity` **16 处 Card 显式指定灰色** |
| K 键长按中文弹出字面「括号」、英文只有 3 对 | 19.24 重构 `longPressSymbolsFor()` **漏了 K 键特判** | `code == "k"` 恒返回 `BracketPairs`（6 对）/ ASCII 6 对 |
| 第四行首键图标不跟随偏好 | 图标没跟 `preferredPage()` 同步 | 图标按偏好解析（九宫格 ↔ 符号网格） |
| 编辑器字段冗余 | 「长按」与「长按气泡」分开 | **合二为一**（单个=直接执行，空格分隔多个=气泡） |
| 第四行比例 | 空格偏窄 | 空格 4.3 → **4.5**、回车 2.0 → **1.8**（主 + 符号键盘，Σ 仍 10.0 保持列对齐） |

## 四、○ 圆环眼睛动画：5 套 → **10 套**

新增：**惊讶**（瞳孔 1.55× + 上抬）· **困倦**（半闭下沉 → 惊醒）· **笑眼**（画成上弧 ∩）·
**转圈看**（上→右→下→左）· **生气**（瞳孔 0.7× + 下压 + 快抖两下）。
为此引入 `eyePupil`（瞳孔缩放）与 `eyeArc`（弧线绘制，Canvas 走 `drawArc` 分支）。

## 五、本阶段最重要的教训：**一次重构引入两处回归**

19.24 那轮改动很大（长文本、符号切换、设置清理、手感），结果：
- 复制条消亡失效（`lastCommittedClip` 与判重口径不一致）
- K 键括号失效（重构 `longPressSymbolsFor` 漏特判）

两处都**躲过了编译检查**，是靠用户实测才暴露的。→ 已写入项目约定：
**重构后必须回归验证"被改函数的上下游交互"，不能只看编译通过。**

## 六、调研：能否用豆包输入法的 API？

产出 `DOUBAO_ASR_REPORT.md`：
- **豆包输入法 App 本身没有开放 API**
- 但火山引擎 ASR 有 **OpenAI 兼容通道（边缘大模型网关）**：与我们的 `web_api` 引擎同构，
  **唯一差别是只吃 `pcm_s16le`**（我们现在上传 WAV 容器）→ 加「PCM 直传」约 20 行即可用
- 原生流式（`wss://openspeech.bytedance.com/api/v3/sauc/bigmodel`，二进制帧 + gzip 分包 100~200ms）
  需 300~500 行 + OkHttp 依赖；Resource ID：1.0 `volc.bigasr.sauc.duration` / 2.0 `volc.seedasr.sauc.duration`

## 七、仓库状态（收工核对）

- 版本：**0.9.42-oime / vc52**（已装机）
- 远程 HEAD：`0db62b33`
- **同步核对：远程 73 / 本地 73，仅本地 0 · 仅远程 0 · 内容不一致 0 → SYNC OK ✅**

## 八、待办

- [ ] 复制条 ✕ 按钮手感反馈（若仍不顺手，考虑"点内容右侧热区"方案）
- [ ] 豆包 ASR 路径 A（PCM 直传）是否要接
- [ ] 耗电 KPI 复核（振动次数 / 熄屏 doze CPU / 后台 CPU）
- [ ] 延后项：targetSdk 36（16KB 页已实测通过）、AudioRecord 实例复用
