# ○输入法 界面风格调研报告

> 需求：给出更多可选界面风格，先调研、再落地。
> 现状：已有 **Material（M3）** 与 **Miuix（MIUI 扁平）**；两者差异目前只在配色层。

---

## 一、结论先行

1. **不要让"风格"散落在 if/else 里**——先把视觉差异抽象成一张 **style token 表**，
   每种风格就是一份 token 数据；新增风格 = 加一行数据，不动渲染代码。
2. **风格只改视觉 token，不动用户调好的几何参数**（键高/行距/列距/圆角/字号）。
   19.33 的教训：给 Miuix 强加圆角与行距，直接顶掉了用户的调校值。
   → 唯一例外：提供「几何也跟随风格」的开关，默认关。
3. **推荐落地顺序**：
   **Miuix 深化** → **Material You 动态取色** → **Glassmorphism（毛玻璃）** → **iOS / Gboard 胶囊** → **像素/贴图皮肤**。

---

## 二、建议的 style token 表（落地骨架）

| token | 含义 | 取值示例 |
|---|---|---|
| `keyCorner` | 键面圆角 | 8dp（Material）/ 14dp（One UI）/ 22dp（胶囊） |
| `keyGap` / `rowGap` | 键间隙 | 4/4（Material）/ 2/2（密集）/ 6/8（松） |
| `keyBorder` | 键边框 | 无 / 1dp 低对比描边 |
| `keyShadow` | 键阴影 | 无 / 2dp 柔和（One UI） |
| `barStyle` | 工具栏 | 同色 / 略深 / 纯白（Miuix） |
| `colorSource` | 色阶来源 | 固定表 / **壁纸取色** / 用户自定义 |
| `accentUsage` | 强调色用法 | 原色 / 柔和混合 |
| `pressEffect` | 按压反馈 | 缩放 / 变暗 / 涟漪 |
| `fontWeight` | 键面字重 | Medium / SemiBold（One UI） |
| `blur` | 背景模糊 | 关 / 开（毛玻璃） |

> 目前 `KeyboardColors` + `buildKeyboardColors(dark)` 已经是"半个 token 表"，扩展成完整 token 表成本可控。

---

## 三、候选风格清单（含实现要点与成本）

### 1. HyperOS / Miuix 深化 ★ 已起步，成本最低
- **视觉**：页面浅灰 `#F2F3F5`、卡片纯白、键面纯白、工具栏白、强调色原色、圆角中等偏大
- **实现**：现有 Miuix 分支再加"键面描边/按压涟漪/字重"几项
- **成本**：低（已在做）｜**风险**：低

### 2. Material You 动态取色（Android 12+）★ 效果最惊艳
- **视觉**：整个键盘色阶**从壁纸取色**（Monet），与系统主题一致
- **实现**：`dynamicLightColorScheme(context)` / `dynamicDarkColorScheme(context)`（API 31+）→
  取出 `primary / surfaceContainer*` 等角色映射到 `KeyboardColors`；低版本回落固定色
- **成本**：中（映射表 + 回退）｜**风险**：低（系统 API）｜**情绪价值**：高

### 3. Glassmorphism 毛玻璃
- **视觉**：键盘半透明 + 模糊背景，键面高光边
- **实现**：Android 12+ 用 `RenderEffect.createBlurEffect` 于键盘背景层；
  低版本退化为半透明（`alpha` 0.7~0.85）+ 1px 亮边
- **成本**：中（模糊层需自绘/`graphicsLayer`）｜**风险**：中（性能；毛玻璃是 GPU 大户，与"打字手感"目标冲突，建议只对工具栏/面板用）

### 4. iOS / Gboard 胶囊风
- **视觉**：键面无明显边框、大圆角胶囊、字重较细、间隙小、整体轻盈
- **实现**：`keyCorner` 拉大（~22dp）、去掉描边与阴影、字重 Normal、行距略增
- **成本**：低（纯 token）｜**风险**：低

### 5. One UI（三星）风
- **视觉**：大圆角 + 柔和阴影 + 半粗字重 + 更亮的键面
- **实现**：给键面加 2dp 柔和阴影（`Modifier.shadow`）+ 字重 SemiBold + 圆角 14~18dp
- **成本**：低-中（阴影在大量键上要注意绘制开销）｜**风险**：低

### 6. Nothing OS 风（单色极简）
- **视觉**：黑白 + 点阵/等宽字体 + 红色单点强调
- **实现**：几乎纯配色 + 字体换成等宽（系统 `monospace`，无需内置字体）
- **成本**：低｜**风险**：低（偏小众）

### 7. 像素 / 复古（8-bit）
- **视觉**：硬边、无圆角、像素字体、高对比
- **实现**：`keyCorner = 0`、关闭反锯齿（`drawStyle` 难控）、像素字体需内置（体积+授权注意）
- **成本**：中（字体资源）｜**风险**：中（字体授权）

### 8. 二次元 / 贴图皮肤（可换肤体系）
- **视觉**：立绘背景 + 图片键帽 + 自定义字体
- **实现**：需要**资源包体系**（zip + 清单 + 键帽九宫格切图）——这是"皮肤引擎"，不是"风格"
- **成本**：**高**（资源格式、切图、适配各键宽、包体）｜**风险**：高
- **建议**：单独立项，不混在风格里

---

## 四、落地建议

| 阶段 | 内容 | 备注 |
|---|---|---|
| **P0** | 抽出 token 表（把现有 Material/Miuix 迁进去） | 不改观感，只重构，便于后续加风格 |
| **P1** | Miuix 深化（描边/字重/按压涟漪） | 已有基础 |
| **P2** | Material You 动态取色（含壁纸取色开关） | 情绪价值最高，系统 API 稳 |
| **P3** | iOS/Gboard 胶囊 + One UI 阴影 | 纯 token，可一起做 |
| **P4** | 毛玻璃（仅工具栏/面板） | 注意性能 |
| **P5** | Nothing OS / 像素 | 小众，可做 |
| **独立项** | 贴图皮肤引擎 | 需资源包体系，另立项 |

**另外建议**：给「几何跟随风格」一个开关（默认关）——想体验"整套风格"的用户可以一次切换，
但**默认不覆盖用户调好的键高/行距**（19.33 的教训）。

---

## 五、需要你定的三个问题

1. **优先做哪两个？**（我的建议：Miuix 深化 + Material You 动态取色）
2. **要不要"几何跟随风格"这个开关？**（默认关）
3. **毛玻璃要不要试？**（好看但吃 GPU，可能影响打字手感——建议只对工具栏/面板开）
